# --- Neo4j (Desktop, local) ---
NEO4J_URI=bolt://...     # you can also use neo4j://127.0.0.1:7687; bolt:// is simplest locally
NEO4J_USER=...                      # or whatever user you set for this DB
NEO4J_PASSWORD=***  # set during DB creation
NEO4J_DATABASE=...                  # unless you created a different DB name

# --- Toggle Global/DRIFT features (requires GDS) ---
ENABLE_GDS=true

# Azure OpenAI Configuration
AZURE_OPENAI_ENDPOINT=https://YOUR-RESOURCE.openai.azure.com
AZURE_OPENAI_API_KEY=your-azure-openai-api-key
AZURE_OPENAI_EMBEDDING_DEPLOYMENT=text-embedding-3-large
EMBEDDING_DIM=3072

# Application Settings
APP_DEFAULT_ASOF=2024-10-01
LOG_LEVEL=INFO

# Development Settings
DEBUG=false
ENVIRONMENT=development

# Optional: For production deployment
# NEO4J_MAX_CONNECTION_POOL_SIZE=100
# NEO4J_CONNECTION_TIMEOUT=30
# AZURE_OPENAI_MAX_RETRIES=3
# AZURE_OPENAI_RETRY_DELAY=1.0
